<h1>Explore Study Groups</h1>
<p>These are study groups that you are not a part of. Join any that interest you!</p>

<h3>Search by course:</h3>

<table>
    <tr>
            <th>Course</th>
            <th>Description</th>
            <th>Start Time</th>
            <th>End Time</th>
            <th>Location</th>
            <th>Capacity</th>
    </tr>

    <% @groups.each do |group| %>
        <tr>
            <td><%= group.category %></td>
            <td><%= group.description %></td>
            <td><%= group.start_time %></td>
            <td><%= group.end_time %></td>
            <td><%= group.location %></td>
            <td><%= group.capacity %></td>
            <td><%= link_to 'Show', controller: "groups", action: "show", id: group %></td>
        </tr>
    <% end %>

    <%= link_to "Create a new study group", { :action => "new" }, class:"btn btn-primary" %>
</table>